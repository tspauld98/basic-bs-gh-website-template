{
    "//": [
        "This package.json file is used to configure the basic grunt project template.",
        "The overrides section is being used to workaround some critical security vulnerabilities in a couple of grunt dependencies."
    ],
    "name": "basic-grunt-template",
    "version": "0.1.0",
    "devDependencies": {
        "grunt": "^1.6.1",
        "grunt-contrib-jshint": "^3.2.0",
        "grunt-contrib-nodeunit": "^5.0.0",
        "grunt-contrib-uglify": "^5.2.2"
    },
    "overrides": {
        "tap": "18.6.1"
    }    
}
